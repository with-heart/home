# Make directory and change into it
function mcd() {
  mkdir -p "$1" && cd "$1"
}

# Test whether command exists
command_exists() {
  type "$1" &> /dev/null;
}

upgrade-node() {
  fnm install latest && cat ~/.fnm/default-packages | grep -v '^#' | xargs -L1 npm install -g
}
